import { UI_Input_Date } from "api/v2-proto/helpers/ui/types/form/inputs/date.js";

// c8 ignore file
export const dob: Partial<UI_Input_Date> = {
  key: "dateOfBirth",
  label: {
    copy: "Date of birth",
  },
};
