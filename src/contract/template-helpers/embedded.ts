/* eslint-disable @typescript-eslint/no-unused-vars */

export default function (bound: Injections, context) {
  /* ============================== *
   * TODO: embedded contracts
   * ============================== */
  return context.fn(this);
}
