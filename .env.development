# Service Configuration
NODE_ENV=development
PORT=3000
DEBUG=apply-flow-service:*

# Debug Configuration
DEBUG_PORT=9229
DEBUG_BREAK_ON_START=false
DEBUG_AUTO_RESTART=true

# Redis Configuration
REDIS_BASE_URL=redis
REDIS_PORT=6379
REDIS_PREFIX=afs_dev
REDIS_KEY_EXPIRATION=86400

# Vault Configuration
VAULT_AUTH_TOKEN=fnord
VAULT_URL=http://vault:9000

# Application Configuration
APPLICATION_SERVICE_URL=http://application-service:3000
CALCULATOR_SERVICE_URL=http://calculator-service:3000
LENDING_DECISION_SERVICE_URL=http://lending-decision-service:3000
PII_TOKEN_SERVICE_URL=http://pii-token-service:3000

# Development Features
ENABLE_DEBUG_LOGGING=true
SKIP_AUTH=false
ENABLE_HOT_RELOAD=true
ENABLE_SOURCE_MAPS=true

# Docker Configuration
COMPOSE_PROJECT_NAME=apply-flow-service
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# Documentation for Environment Variables:
# NODE_ENV: Environment mode (development/production)
# PORT: Port the service runs on
# DEBUG: Debug patterns for logging
# DEBUG_PORT: Port for Node.js debugger
# DEBUG_BREAK_ON_START: Break on first line when debugging
# DEBUG_AUTO_RESTART: Auto restart on file changes
# REDIS_BASE_URL: Redis host
# REDIS_PORT: Redis port
# REDIS_PREFIX: Prefix for Redis keys
# REDIS_KEY_EXPIRATION: TTL for Redis keys in seconds
# VAULT_AUTH_TOKEN: Authentication token for Vault
# VAULT_URL: URL for Vault service
# APPLICATION_SERVICE_URL: URL for application service
# CALCULATOR_SERVICE_URL: URL for calculator service
# LENDING_DECISION_SERVICE_URL: URL for lending decision service
# PII_TOKEN_SERVICE_URL: URL for PII token service
# ENABLE_DEBUG_LOGGING: Enable detailed logging
# SKIP_AUTH: Skip authentication (development only)
# ENABLE_HOT_RELOAD: Enable hot reload for development
# ENABLE_SOURCE_MAPS: Enable source maps for debugging
# COMPOSE_PROJECT_NAME: Docker Compose project name
# DOCKER_BUILDKIT: Enable BuildKit for Docker builds
# COMPOSE_DOCKER_CLI_BUILD: Use Docker CLI for builds