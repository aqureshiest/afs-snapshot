{{!
  User state is fetched depending on the auth artifacts present
}}
{{#with (contract 'auth') as |auth|}}
  {{#if auth.artifacts.userId}}
  {
    "redisMethod": "getUserState",
    "key": {{{ json auth.artifacts.userId}}}
  }
  {{else}}
    null
  {{/if}}
{{else}}
  null
{{/with}}

