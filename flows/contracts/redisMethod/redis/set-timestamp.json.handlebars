{{#if auth.artifacts.userId}}
  {
    "redisMethod": "setUserState",
    "key": "{{{auth.artifacts.userId}}}",
    "value": {
      "decision-requested-at": "{{getCurrentTime}}"
    }
  }
{{else}}
  null
{{/if}}
