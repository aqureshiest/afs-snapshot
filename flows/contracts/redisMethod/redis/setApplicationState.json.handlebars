{{#if application.id}}
{
  "redisMethod": "setApplicationState",
  "key": "{{{application.id}}}",
  "value": {
    "manifest": "{{{request.originalUrl}}}",
    "step": "{{request.body.values.step}}"
  }
}
{{/if}}