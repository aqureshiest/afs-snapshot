{{#with (coalesce request.headers.idtoken request.headers.idToken) as |token|}}
{
  "client": "NeasClient",
  "method": "post",
  "uri": "/auth/interservice/verify",
  "body": {
    "token": {{{ json token }}}
  }
}
{{else}} null {{/with}}
