{{#with (contract 'actions' (sum @index -1)) as |event|}}
  {
    "action": "unauthenticated",
    "client": "NeasClient",
    "method": "post",
    "uri": "/auth/identity/unauthenticated",
    "body": {
      "applicationId": {{{ json event.id }}}
    }
  }
{{else}}
  null
{{/with}}
