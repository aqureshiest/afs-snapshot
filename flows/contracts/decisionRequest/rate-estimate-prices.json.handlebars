{{#if request.params.id}}
  {{#with (getMostRecentRateInquiry application.applicant.details.decision) as |decision| }}
    {
      "decisionRequestMethod": "getPaymentsAndRates",
      "id": "{{decision.decisionID}}",
      "payload": {
        "type": "decisioning-request"
      }
    }
  {{/with}}
{{else}}
  null
{{/if}}