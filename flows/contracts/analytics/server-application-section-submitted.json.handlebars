{{#if request.params.id}}
  {{#with request.body.values}}
    {
      {{#if (or currentAddress employmentType annualIncome financialAccounts addAddtionalIncome public_token accounts) }}
        "type": "track",
      {{else}}
        "type": "",
      {{/if}}
      "payload": {
        {{#if currentAddress }}
          "section": "primary-info",
        {{else if employmentType }}
          "section": "employment type",
          "employment_type": "{{employmentType}}",
        {{else if (or annualIncome addAddtionalIncome)}}
          "section": "income",
        {{else if public_token}}
          "section": "income verification",
          "income_verification_method": "plaid",
        {{else if accounts}}
          "section": "income verification",
          "income_verification_method": "manual",
        {{/if}}
        "event": "Server Application Section Submitted"
      }
    }
  {{/with}}
{{/if}}