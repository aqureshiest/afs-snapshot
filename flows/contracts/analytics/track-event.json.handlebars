
{{#if request.body.values.analytics}}
  {{#with request.body.values.analytics}}
    {
      "type": "{{type}}",
      "payload": {
        "event": "{{event}}",
        "section": "{{section}}",
        "name": "{{name}}",
        "title": "{{title}}",
        "source": "{{source}}",
        "action": "{{action}}",
        "text": "{{text}}",
      }
    }
  {{/with}}
{{else}}
  null
{{/if}}