
{{#if request.body.values}}
  {{#with request.body.values}}
    {
      "type": "{{type}}",
      "payload": {
        "event": "{{event}}",
        "section": "{{section}}",
        "name": "{{name}}",
        "title": "{{title}}",
        "source": "{{source}}",
        "action": "{{action}}",
        "text": "{{text}}",
      }
    }
  {{/with}}
{{else}}
  null
{{/if}}