
{{#if request.body.values}}
  {{#with request.body.values}}
    {
      "type": "{{type}}",
      "event": "{{name}}",
      "anonymousId": "{{anonymousId}}",
      "payload": {{{json properties}}}
    }
  {{/with}}
{{else}}
  null
{{/if}}