{
    "key": "student-info",
    "type": "form",
    "label": "Student Info",
    "status": "notStarted",
    "showProgress": true,
    "pageLayout":{{{json (contract 'pageLayout')}}},
    "steps": {{{json (contract 'steps')}}},
    "validation": {
        "type": "object",
        "properties": {
            "basic": {
                "type": "object",
                "properties": {
                    "dateOfBirth": {{{json (schema 'questions/primary/dateOfBirth')}}},
                    "phone": {{{json (schema 'questions/primary/phone')}}}
                }
            },
            "currentAddress": {
                "type": "object",
                "properties": {
                    "currentAddress": {{{json (schema 'questions/primary/address')}}},
                    "sixMonthResidence": {{{json (schema 'questions/primary/address/additional-address')}}}
                }
            },
            "previousAddress": {
                "type": "object",
                "properties": {
                    "previousAddress": {{{json (schema 'questions/primary/address')}}}
                }
            },
            "employmentStatus": {
                "type": "object",
                "properties": {
                    "employmentType": {{{json (schema 'questions/primary/employment')}}}
                }
            },
            "income": {
                "type": "object",
                "properties": {
                    "employerName": {{{json (schema 'questions/primary/employment/employer-name')}}},
                    "jobTitle": {{{ json (schema 'questions/primary/employment/job-title')}}},
                    "annualIncome": {{{json (schema 'questions/primary/income/amount/annual-income')}}},
                    "claimedAssets": {{{json (schema 'questions/primary/income/amount/claimed-assets')}}},
                    "additionalAmount": {{{json (schema 'questions/primary/income/amount/additional-income')}}}
                }
            },
            "incomeSelfEmployed": {
                "type": "object",
                "properties": {
                    "jobTitle": {{{ json (schema 'questions/primary/employment/job-title')}}},
                    "startDate": {{{ json (schema 'questions/primary/employment/start-date')}}},
                    "annualIncome": {{{json (schema 'questions/primary/income/amount/annual-income')}}},
                    "claimedAssets": {{{json (schema 'questions/primary/income/amount/claimed-assets')}}},
                    "additionalAmount": {{{json (schema 'questions/primary/income/amount/additional-income')}}}
                }
            },
            "incomeFutureEmployment": {
                "type": "object",
                "properties": {
                    "employerName": {{{json (schema 'questions/primary/employment/employer-name')}}},
                    "jobTitle": {{{ json (schema 'questions/primary/employment/job-title')}}},
                    "startDate": {{{ json (schema 'questions/primary/employment/start-date')}}},
                    "annualIncomeFuture": {{{json (schema 'questions/primary/income/amount/annual-income')}}},
                    "claimedAssets": {{{json (schema 'questions/primary/income/amount/claimed-assets')}}},
                    "additionalAmount": {{{json (schema 'questions/primary/income/amount/additional-income')}}}
                }
            },
            "sourcesOfIncome": {
                "type": "object",
                "properties": {
                    "sourcesOfIncome": {{{json (schema 'questions/primary/income/sources-of-income-retired')}}}
                }
            },
            "additionalIncome": {
                "type": "object",
                "properties": {
                    "addAddtionalIncome": {{{json (schema 'questions/primary/income/has-additional-income')}}},
                    "additionalAmount": {{{json (schema 'questions/primary/income/amount/additional-income')}}}
                }
            },
            "consistentIncome": {
                "type": "object",
                "properties": {
                    "hasConsistentIncome": {{{json (schema 'questions/primary/income/consistent-income')}}}
                }
            },
            "hasConsistentIncome": {
                "type": "object",
                "properties": {
                    "annualIncome": {{{json (schema 'questions/primary/income/amount/annual-income')}}},
                    "additionalAmount": {{{json (schema 'questions/primary/income/amount/additional-income')}}}
                }
            }
        }
    }
}