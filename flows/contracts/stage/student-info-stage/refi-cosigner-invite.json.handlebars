{
    "key": "full-refi-cosigner-invite",
    "type": "form",
    "label": "Cosigner Invite",
    "status": "notStarted",
    "showProgress": true,
    {{#if (contract "analytics")}}
        "analytics": {{{json (contract 'analytics')}}},
    {{/if}}
    "pageLayout":{{{json (contract 'pageLayout')}}},
    "seo": {
        "pageTitle": "Earnest"
    },
    "steps": {{{json (contract 'steps')}}},
    "validation": {
        "type": "object",
        "properties": {
            "cosigner-invite": {
                "type": "object",
                "properties": {
                    "name": {{{json (schema 'questions/cosigner/name')}}},
                    "email": {{{json (schema 'questions/cosigner/email')}}}
                }
            }
        }
    }
}