{
  "type": "view",
  "id": "{{{request.params.id}}}",
  "definition": {
    "type": "view",
    "definition": {
        "key": "income-verification-review",
        "financialAccounts":
          {{#list}}
            {{#each application.details.financialAccounts}}
             {"name": "{{this.name}}", "account_last4": "{{this.account_last4}}" }
            {{/each}}
          {{/list}}
        
    }
  }
}

