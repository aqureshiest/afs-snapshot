{{#if request.params.id}}
{
  "type": "view",
  "id": "{{request.params.id}}",
  "definition": {
    "type": "bankAccounts",
    "definition": {
      "progress": 90,
      "key": "bankAccounts",
      "header": {
        "copy": "Let's verify your income.",
        "subcopy": "Link the account(s) where your paycheck is deposited for accurate eligibility and rates assessment."
      },
      "list": {
          "type": "ul",
          "defaultIcon": {
              "type": "NucleusIcon",
              "name": "CheckmarkCircleFilledIcon"
          },
          "items": [
              {
                  "copy": "Speeds up loan approval by up to 3 days"
              },
              {
                  "copy": "Personal information secured with end-to-end encryption"
              }
          ]
      },
      "trustpilot": false,
        "plaidOptions": {
          "linkToken": {{{ json (contract 'method' )}}},
          "exchangeUrl": "plaid/exchange/{{{application.id}}}"
        },
      
      "primaryAction": {
        "copy": "Move to review",
        "action": {
          "key": "landingViewed",
          "type": "navigate",
          "properties": {
            "manifest": "save-details/financials/{{{request.params.id}}}"
          }
        }
      },
      "accounts":
      {{#list}}
        {{#each application.primary.details.financialAccounts}}
          {"index": {{{ number this.index}}}, "name": "{{this.name}}", "account_last4": "{{this.account_last4}}", "type": "{{this.type}}", "selected": "{{this.selected}}" }
        {{/each}}
      {{/list}}
    }
  }
}
{{else}}
{
  "statusCode": 401,
  "message": "Id param is required."
}
{{/if}}