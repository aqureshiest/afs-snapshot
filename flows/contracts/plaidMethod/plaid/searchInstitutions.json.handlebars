{{#if request.body.values.query}}
  {
    "plaidMethod": "searchInstitutions",
    "id": "-",
    "payload": {
      "query": "{{request.body.values.query}}"
      }
  }
{{else}} 
{  
  "error": "Missing query param" 
}
{{/if}}
