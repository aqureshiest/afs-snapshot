{
    {{#if application.reference.lendingDecisionID}}
      "action": {
        "type": "navigate",
        "properties": {
          "goTo": "/_/dashboard/"
        }
      }
    {{else}}
      "manifest": "{{{manifest.name}}}",
      "id": "{{application.id}}",
      "stage": {{{ json (contract 'stage')}}}
    {{/if}}
}
