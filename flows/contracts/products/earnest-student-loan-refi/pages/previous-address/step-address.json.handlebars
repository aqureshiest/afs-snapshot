{
    "key": "previous-address",
    "label": "Previous Address",
    "back": {
        "action": {{{ json (contract 'back-action') }}}
    },
    "submit": {
        "copy": {{{json (contract 'save-copy')}}},
        "action": {{{json (contract 'save-action')}}}
    },
    "progress": 50,
    "rows": {{{json (contract 'address-rows')}}}
}