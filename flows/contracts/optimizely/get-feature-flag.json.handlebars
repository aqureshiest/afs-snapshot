{{log "===== headers" request.headers.device_uuid}}
{
  "optimizelyMethod": "getFeatureFlags",
  "userId": {{#if (isNull auth.artifacts.userId)}}"{{{request.headers.device_uuid}}}"{{else}}"{{auth.artifacts.userId}}"{{/if}},
  "featureFlagKeys": {{{json @ref.flags}}}
}