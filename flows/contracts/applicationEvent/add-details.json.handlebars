{{#if request.params.id}}
  {
    "event": "addDetails",
    "payload": {
      "id": "{{request.params.id}}",
      "details": {{{json request.body.values}}}
    },
    "fields": ["application.id"]
  }
{{else}}
  null
{{/if}}