{{#if application.id}}
  {
    "event": "addDetails",
    "payload": {
      "id": "{{application.id}}",
      "details": {{{json values}}}
    },
    "fields": ["application.id"]
  }
{{else}}
  null
{{/if}}