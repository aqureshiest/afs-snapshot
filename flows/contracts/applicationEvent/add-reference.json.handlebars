
{{#if request.params.id}}
  {
    "event": "addReferences",
    "payload": {
      "id": "{{request.params.id}}",
      {{#with request.body.values}} {{! body.values.ssn}}
      "references": 
        {{#list}} {{! make sure to flatten each element into a single line}}
          {{#if (eq referenceTyp e "ssn"}}
            {"referenceId": {{{}}}}
          {{/if}}
        {{/list}}
    },
    {{/with}}
    "fields": "application { id }"
  }
{{else}}
  null
{{/if}}