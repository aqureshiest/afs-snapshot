{
    "key": "checkout",
    "type": "navigate",
    "properties": {
        {{#if this.application.monolithLoanID}}
            "goTo": "{{env.BASE_URL}}/_/lending-checkout-ui/congratulations?brand=Earnest&product=SLR&referenceLoanId={{this.application.monolithLoanID}}",
        {{else}}
            "goTo": "{{env.BASE_URL}}/_/lending-checkout-ui/congratulations?brand=Earnest&product=SLR&applicationId={{this.application.id}}",
        {{/if}}
        "external": true
    }
}