{
    "key": "rate-check-request",
    "type": "request",
    "properties": {
        "manifest": "rate-estimate/primary/rate-request/{{application.primary.id}}",
        "analytics": {},
        "method": "POST",
        "onError": {{{json (contract 'onError')}}}
    }
}