{
  "key": "rate-estimate-status",
  "type": "request",
  "properties": {
    "manifest": "rate-estimate/rate-status/{{application.id}}",
    "method": "POST",
    "onError": {{{json (contract 'onError')}}}
  }
}
