{
    "key": "rate-check-request",
    "type": "request",
    "properties": {
        "manifest": "rate-estimate/cosigner/rate-request/{{application.id}}",
        "analytics": {},
        "method": "POST",
        "onError": {{{json (contract 'onError')}}}
    }
}