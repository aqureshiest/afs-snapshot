{
  {{#if (contract "copy")}}
    "copy": {{{json (contract 'copy')}}},
  {{/if}}
  {{#if (contract "text")}}
    "text": {{{json (contract 'text')}}},
  {{/if}}
  {{#if (contract 'conditional')}}"conditional": {{{json (contract 'conditional')}}},{{/if}}
  "action": {{{json (contract 'action')}}}
}