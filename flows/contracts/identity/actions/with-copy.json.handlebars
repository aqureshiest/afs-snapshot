{
  {{#if (contract "copy")}}
    "copy": {{{json (contract 'copy')}}},
  {{/if}}
  {{#if (contract "text")}}
    "text": {{{json (contract 'text')}}},
  {{/if}}
  "action": {{{json (contract 'action')}}}
}