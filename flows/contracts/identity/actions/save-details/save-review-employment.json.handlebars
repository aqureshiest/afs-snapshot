{{! redirect to review}}
{
    "key": "save-details",
    "type": "request",
    "properties": {
        "manifest": "save-details/save-employment-info-review/{{application.primary.id}}",
        "method": "POST",
        "onError": {{{json (contract 'onError')}}}
    }
}