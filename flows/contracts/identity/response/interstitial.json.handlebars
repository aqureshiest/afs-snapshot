{{!
  Use this as the root contract for an interaction
}}
{{#if (and (contract 'requirements'))}}
  {
    "action": {
      "key": "submitInterstitial",
      "type": "interstitial",
      "properties": {
        "showSpinner": true,
        "header": { "copy": "We’re reviewing your application. This can take up to a minute." },
        "action": {{{json (contract 'poll')}}}
      }
    },
    "result": {{{json (contract 'actions')}}},
    "error": {{{json error}}}
  }
{{else}}
  {
    "action": {{{ json (contract 'default') }}}
  }
{{/if}}
