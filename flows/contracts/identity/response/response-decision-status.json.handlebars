
{{#if application.id}}
{
  "action": {
    "key": "submitInterstitial",
    "type": "interstitial",
    "properties": {
      "showSpinner": true,
      "header": { "copy": "One moment. We’re reviewing your application." },
      "actionDelay": 3000,
      "action": 
      {{#if (eq application.status.name 'submitted') }}
        {{{json (contract 'homebaseAction')}}}
      {{else if (eq application.status.name 'approved')}}
        {{{json (contract 'checkoutAction')}}}
      {{else}}
        {{! use home base analytics redirect contract to track when status is something else besides submitted or approved}}
        {{{json (contract 'homebaseActionAnalytics')}}}
      {{/if}}
    }
  }
}
{{else}}
  null
{{/if}}