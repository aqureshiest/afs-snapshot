{{!
  Generic response for creating a new application resource or returning it
  from the inputs
}}
{{#obj}}
  {{{ json @ref.application }}}
  {{#each @ref.extras}}
    {{#if extra}}
      {{#multiline~}}
        {{{ json extra }}}
      {{~/multiline}}
    {{/if}}
  {{/each}}
{{/obj}}
