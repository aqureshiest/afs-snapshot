{{#if (eq request.body.values.hasConsistentIncome "false")}}
{
  "type": "navigate",
  "properties": {
    "goTo": {{{ json (contract 'redirect') }}}
  },
  "actions": {{{ json (contract 'actions') }}}
}
{{else}}
    null
{{/if}}