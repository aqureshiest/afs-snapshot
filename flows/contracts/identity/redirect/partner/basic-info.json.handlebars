{
  "action": {
    "type": "navigate",
    "properties": {
      "goTo": "/{{application.brand}}/student-loan-refi/{{#if (isNull application.reference.partnerId)}}previous-address{{else}}basic-info-partner{{/if}}/{{request.params.id}}"
    }
  },
  "result": {{{ json (contract 'actions') }}},
  "error": {{{json error}}}
}
