{{#if request.params.id }}
  {{#if (hasValues applicationState) }}
    {{#with applicationState }}
      {{#if step}}
        "{{manifest}}?step={{step}}"
      {{else}}
        "{{manifest}}"
      {{/if}}
    {{/with}}
  {{else}}
    "{{application.brand}}/{{application.product}}{{#if (eq application.applicant.role "cosigner")}}/cosigner{{/if}}/{{application.id}}"
  {{/if}}
{{/if}}
