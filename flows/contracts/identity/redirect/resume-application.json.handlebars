{{#if request.params.id }}
  {{#if (hasValues applicationState) }}
    {{#with applicationState }}
      {{#if step}}
        "{{manifest}}?step={{step}}"
      {{else}}
        "{{manifest}}"
      {{/if}}
    {{/with}}
  {{else}}
    "{{application.brand}}/{{{json (mapProductForRedirect application.product)}}}{{#if (eq application.applicant.role "cosigner")}}/cosigner{{/if}}/{{application.id}}"
  {{/if}}
{{else}}
  null
{{/if}}
