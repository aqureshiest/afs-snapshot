{
  "action": {
    "type": "navigate",
    "properties": {
      "goTo": "{{env.BASE_URL}}/_/auth/neasauth/instant_verify?email={{lookup (lookup (getApplicant request.params.id application) "details") "email"}}"
    }
  }
}
