{
    "action": {
        "type": "navigate",
        "properties": {
            {{#if (eq request.body.values.citizenship 'non-resident')}}
                "goTo": "earnest/student-loan-refi/unsupported-citizenship/{{application.applicant.id}}"
            {{else}}
                "goTo": "earnest/student-loan-refi/income/{{application.applicant.id}}"
            {{/if}}
        }
    },
    "result": {{{ json (contract 'actions') }}},
    "error": {{{json error}}}
}
