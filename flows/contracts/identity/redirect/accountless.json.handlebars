
  {
    "action": {
      "type": "navigate",
      "properties": {
        "goTo": "earnest/student-loan-refi/basic-info/{{application.applicants.0.id}}"
      }
    },
    {{! returning actions is not needed, but left for debugin }}
    "result": {{{ json (contract 'actions') }}},
    "error": {{{json error}}}
  }
