{
  "action": {
    "type": "navigate",
    "properties": {
      "goTo": "earnest/full-refi/basic-info/{{application.id}}"
    }
  },
  {{! returning actions is not needed, but left for debugin }}
  "result": {{{ json (contract 'actions') }}},
  "error": {{{json error}}}
}

