{
    "type": "info",
    "definition": {
        "key": {{#if (contract "key")}}{{{json (contract "key")}}}{{else}}"header"{{/if}},
        "type": "infoHeader",
        "header": {
            "copy": {{#if (contract "copy")}}{{{json (contract "copy")}}}{{else}}"Header"{{/if}},
            "subCopy": {{#if (contract "subCopy")}}{{{json (contract "subCopy")}}}{{else}}""{{/if}}
        }
    }
}