{
    "key": "income",
    "label": "Student's citizenship status info",
    "back": {
        "action": {{{ json (contract 'back-button') }}}
    },
    "submit": {
        {{#if (eq application.applicant.role "primary")}}
            "copy": "Refinance Amount",
        {{else}}
            "copy": "Confirm Identity",
        {{/if}}
        "action": {{{json (contract 'save_income_details_action')}}}
    },
    "progress": {{{json (contract 'progress')}}},
    "rows": {{{json (contract 'student-info-income-rows')}}}
}
