{
    "key": "incomeSource",
    "label": "Student's income source info",
    "back": {
        "action": {{{ json (contract 'back-button') }}}
    },
    "submit": {
            "copy": {{{ json (contract 'submit_copy') }}},
            "action": {{{json (contract 'save_details_action')}}}
    },
    "progress": 81,
    {{#if userState.additionalAmount}}
        "rows": {{{json (contract 'income-source-rows')}}}
    {{else}}
        "rows": {{{json (contract 'income-source-rows-without-additional-income')}}}
    {{/if}}
}