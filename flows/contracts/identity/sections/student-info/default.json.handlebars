{
    "label": "Student info",
    "status": {{{ contract 'student-info-section-status'}}},
    "progress": 10,
    "action": {
        "key": "",
        "type": "navigate",
        "properties": {
            {{!TODO: need to find a way to get the primary out of applicants      }}
            {{!    for now, it assumes the root application only has 1 applicant  }}
            {{!    since its trying to load the solo syllabus                     }}
            "goTo": "student-loans/student-info/{{application.applicants.0.id}}"
        }
    }
}
