{
  "type": "string",
  "minLength": 1,
  "format": "email",
  "deepEmailValidator": true,
  "errorMessage": {
    "minLength": "Email is required.",
    "type": "Invalid email.",
    "format": "Email address contains invalid characters."
  }
}