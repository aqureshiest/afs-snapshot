{
  "type": "array",
  "items":
  {
    "type": "object",
    "properties": {
      "number": {
          "type": "string",
          "minLength": 10,
          "maxLength": 10,
          "pattern": "^[0-9]+$",
          "errorMessage": {
              "minLength": "Phone number is required",
              "maxLength": "Phone number is required",
              "pattern": "Invalid phone number",
              "type": "Phone number is required"
          }
      },
      "index": {
        "type": "string",
        "minLength": 3,
        "maxLength": 3,
        "pattern": "^[0-9]+$",
        "errorMessage": {
            "minLength": "Area code is required",
            "maxLength": "Invalid area code",
            "pattern": "Invalid phone number",
            "type": "Phone is required"
        }
      },
      "type": {
        "type": "string",
        "enum": ["mobile", "work", "home"],
        "errorMessage": {
            "enum": "Value must be one of 'mobile', 'work', 'home' "
        }
      }
    },
    "required": [
        "number"
    ]
  }
}