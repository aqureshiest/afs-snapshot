{{#with application.primary}}
  {
    "type": "object",
    "properties": {
      "street": {{{json (contract 'address-field-previous-address-street')}}},
      "apt": {{{json (contract 'address-field-apt')}}},
      "city": {{{json (contract 'address-field-city')}}},
      "state": {{{json (contract 'address-field-state')}}},
      "zip": {{{json (contract 'address-field-zip')}}}
    },
    "required": ["street", "city", "state", "zip"]
  }
{{/with}}
