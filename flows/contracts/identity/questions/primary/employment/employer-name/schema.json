{
    "type": "string",
    "pattern": "^[\\x00-\\x7F]*$",
    "minLength": 1,
    "errorMessage": {
        "minLength": "Employer name must be longer than 1 character",
        "pattern": "Invalid employer name",
        "type": "Invalid employer name"
    }
}