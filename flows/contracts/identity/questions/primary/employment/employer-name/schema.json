{
  "type": "string",
  "pattern": "^[a-zA-Z0-9\\s.'-]*$",
  "minLength": 1,
  "errorMessage": {
    "minLength": "Employer name is required.",
    "pattern": "Employer name contains invalid characters.",
    "type": "Invalid employer name."
  }
}