{
    "type": "string",
    "pattern": "^[\\x00-\\x7F]*$",
    "minLength": 1,
    "errorMessage": {
        "minLength": "Job title must be longer than 1 character",
        "pattern": "Invalid Job title",
        "type": "Invalid Job title"
    }
  }