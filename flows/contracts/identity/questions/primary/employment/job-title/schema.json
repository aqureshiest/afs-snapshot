{
  "type": "string",
  "pattern": "^[a-zA-Z\\s.'-]*$",
  "minLength": 1,
  "errorMessage": {
    "minLength": "Job title is required.",
    "pattern": "Job title contains invalid characters.",
    "type": "Invalid Job title."
  }
}