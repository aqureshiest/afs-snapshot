{
    "type": "string",
    "pattern": "^[\\x00-\\x7F]*$",
    "minLength": 1,
    "errorMessage": {
        "minLength": "Job title is required",
        "pattern": "Invalid Job title",
        "type": "Invalid Job title"
    }
  }