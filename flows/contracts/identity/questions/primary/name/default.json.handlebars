{{#with application.primary}}
{
    "type": "input",
    "definition": {
        "key": "name",
        "type": "inputName",
        "status": {{{json (contract 'primary_name_input_status')}}},
        "value": {{{json details.name}}},
        "label": {
            "copy": "Full legal name"
        },
        "error": "{{{ ajv 'errors' 'questions/primary/name' details.name }}}"
    }
}
{{/with}}
