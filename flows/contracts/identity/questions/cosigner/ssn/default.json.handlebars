{
    "type": "input",
    "definition": {
        "key": "ssn",
        "type": "inputSsn",
        "status": {{{json (contract 'cosigner_ssn_input_status')}}},
        {{#with (contract 'decryptPiiSsn') as |result|}}
            "value": {{{json result.results.value}}},
        {{/with}}
        "obfuscate": true,
        "label": {
            "copy": "Social security number"
        },
        "error": "{{{ ajv 'errors' 'questions/cosigner/ssn' application.cosigner.ssnTokenURI }}}"
    }
}
