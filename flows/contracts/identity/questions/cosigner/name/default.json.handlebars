{
    "type": "input",
    "definition": {
        "key": "name",
        "type": "inputName",
        "placeholders": {
            "first": "First name",
            "last": "Last name"
        },
        "status": {{{json (contract 'cosigner_name_input_status')}}},
        "value": {
            "first": {{#if applicant.details.name.first}}{{{json applicant.details.name.first}}}{{else}}""{{/if}},
            "last": {{#if applicant.details.name.last}}{{{json applicant.details.name.last}}}{{else}}""{{/if}}
        },
        "label": {
            "copy": "Cosigner's full legal name"
        },
        "error": {{#if application.cosigner}} "{{{ ajv 'errors' 'questions/cosigner/name' application.cosigner.details.name }}}" {{else}} "" {{/if}}
    }
}

