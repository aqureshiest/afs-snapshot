{
    "type": "input",
    "definition": {
        "key": "name",
        "type": "inputName",
        "placeholders": {
            "first": "First name",
            "last": "Last name"
        },
        "status": {{{json (contract 'cosigner_name_input_status')}}},
        "value": {
            "first": {{#if cosigner.details.name.first}}{{{json cosigner.details.name.first}}}{{else}}""{{/if}},
            "last": {{#if cosigner.details.name.last}}{{{json cosigner.details.name.last}}}{{else}}""{{/if}}
        },
        "label": {
            "copy": "Cosigner's full legal name"
        },
        "error": {{#if application.cosigner}} "{{{ ajv 'errors' 'questions/cosigner/name' application.cosigner.details.name }}}" {{else}} "" {{/if}}
    }
}

