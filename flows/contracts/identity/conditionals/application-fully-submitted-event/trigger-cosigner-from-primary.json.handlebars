{{!-- Triggers cosigner event from primary submission --}}
{{#if (eq (mapLoanType application.tag.applicants) "cosigned")}}
  {{#if (eq application.cosigner.status.name "submitted")}}
    {{json (contract "event")}}
  {{else}}
    {}
  {{/if}}
{{else}}
 {}
{{/if}}