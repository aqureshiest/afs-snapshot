{{!-- Trigger primary event from cosigner submission --}}
{{#if (eq (mapLoanType application.tag.applicants) "cosigned")}}
  {{#if (eq application.primary.status.name "submitted")}}
    {{json (contract "event")}}
  {{else}}
    {}
  {{/if}}
{{else}}
 {}
{{/if}}