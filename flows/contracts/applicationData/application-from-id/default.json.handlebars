{{!
  The contract key 'application' can be any contract that resolves with an 'id'
  as a top-level key that corresponds to an application
}}
{
  "id": {{{ json (coalesce @ref.application.id request.params.id) }}},
  "query": {{{ json @ref.query }}},
  "root": true
}
