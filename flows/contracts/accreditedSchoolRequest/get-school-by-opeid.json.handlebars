{{#with (contract "opeid") as |opeid|}}
  {{#if (and opeid (ne opeid "high_school"))}}
  {
    "accreditedSchoolServiceRequestMethod": "getSchoolByOpeid",
    "opeid": {{{json opeid}}}
  }
  {{else}}
    "high_school"
  {{/if}}
{{else}}
  null
{{/with}}
